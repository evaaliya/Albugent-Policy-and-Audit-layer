# Pinned <2.0.0: mcp 2.0.0 (released 2026-07-28) renamed FastMCP -> MCPServer and
# streamablehttp_client -> streamable_http_client with a different signature. Both
# the 1.x and 2.x lines already satisfy the Alexa+ MCP Toolkit's requirement of
# Streamable HTTP + MCP spec 2025-11-25+ -- this pin is purely to keep this repo's
# code (mcp_server/server.py, demo/simulate_session.py) valid as written.
mcp[cli]>=1.10,<2.0.0
fastapi>=0.115
uvicorn>=0.30
httpx>=0.27
